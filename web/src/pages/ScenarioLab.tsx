import { useEffect,useState } from 'react'; import { load,type ResultsData } from '../shared/data';
export function ScenarioLab(){const [data,setData]=useState<ResultsData>();useEffect(()=>{load().then(setData)},[]);const points=data?.demo_scenarios[0]?.scenario.customers||[];return <><h1>Scenario Lab</h1><section className="panel"><p>Preview: {points.length} customer coordinates from a seeded engine scenario.</p><svg viewBox="0 0 100 100" width="100%" height="360" aria-label="Scenario coordinate preview"><rect width="100" height="100" fill="#0B0F0A"/>{points.map((p:number[],i:number)=><circle key={i} cx={p[0]} cy={p[1]} r="1.1" fill="#39FF88"/>)}<circle cx="0" cy="0" r="2" fill="#B4FF39"/></svg><button>Requires the backend</button></section></>}
