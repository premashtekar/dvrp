import { useEffect,useState } from 'react'; import { load,type ResultsData } from '../shared/data'; import { Loading,PageHead } from '../shared/Layout';
export function ScenarioLab(){const [data,setData]=useState<ResultsData>();useEffect(()=>{load().then(setData).catch(()=>setData(undefined))},[]);if(!data)return <><PageHead title="Scenario Lab"><p className="page-subtitle">Loading a seeded engine scenario.</p></PageHead><Loading/></>;const scenario=data.demo_scenarios[0].scenario;return <><PageHead title="Scenario Lab"><p className="page-subtitle">Coordinate preview from one stored demo scenario. Live scenario creation is separate from this static export.</p></PageHead><div className="grid grid-2"><section className="panel"><p className="section-kicker">Stored scenario preview</p><h2 className="section-title">{scenario.customers.length} customer coordinates</h2><svg viewBox="-4 -4 108 108" width="100%" height="390" aria-label="Seeded customer coordinate preview" style={{marginTop:12,background:'#0B0F0A',borderRadius:8}}><path d="M0 20h100M0 40h100M0 60h100M0 80h100M20 0v100M40 0v100M60 0v100M80 0v100" stroke="#243024" strokeWidth=".3"/>{scenario.customers.map((p:number[],i:number)=><circle key={i} cx={p[0]} cy={p[1]} r="1.25" fill="#39FF88" opacity=".8"/>)}<circle cx="0" cy="0" r="2.5" fill="#B4FF39"/></svg></section><section className="panel backend-card"><div className="brand-mark">↗</div><div><p className="section-kicker">Live actions</p><h2 className="section-title">Requires the backend</h2><p className="muted">Creating, changing, or executing a scenario needs the engine service. This page only previews persisted engine output.</p><button className="button secondary" disabled style={{marginTop:10,opacity:.65}}>Create live scenario</button></div></section></div></>}
