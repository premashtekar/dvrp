import type { ReactNode } from 'react'; import { Link, useLocation } from 'react-router-dom';
const links=[['/overview','Overview'],['/lab','Scenario Lab'],['/replay','Replay'],['/compare','Compare'],['/results','Results'],['/experiments','Experiments'],['/methodology','Methodology']];
export function Layout({children}:{children:ReactNode}){const loc=useLocation();return <><header style={{borderBottom:'1px solid var(--border)',padding:'12px 5vw',display:'flex',gap:14,alignItems:'center',flexWrap:'wrap'}}><strong style={{color:'var(--lime)'}}>DVRP Research Lab</strong>{links.map(([path,label])=><Link key={path} to={path} style={{color:loc.pathname===path?'var(--mint)':'var(--fg)',textDecoration:'none'}}>{label}</Link>)}<span className="mono" style={{marginLeft:'auto',fontSize:12,border:'1px solid var(--border)',padding:6}}>Precomputed from real engine runs (n=5 seeds)</span></header><main style={{maxWidth:1400,margin:'auto',padding:'28px 5vw'}}>{children}</main></>}
